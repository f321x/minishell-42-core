{
    "version": "0.2.0",
    "configurations": [
        {
            "name": "Launch",
            "type": "lldb",
            "program": "${workspaceFolder}/minishell",
            "args": [],
            "cwd": "${workspaceFolder}",
            "request": "launch",
            "MIMode": "lldb",
            "externalConsole": true
        }
    ]
}
